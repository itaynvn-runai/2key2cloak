apiVersion: v1
kind: Service
metadata:
  name: {{ include "2key2cloak.fullname" . }}
  labels:
    {{- include "2key2cloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "2key2cloak.selectorLabels" . | nindent 4 }}
